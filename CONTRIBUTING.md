# Contributing

Run the repository's canonical validation command before requesting review:

```sh
npm run check
```

This command is the shared entry point even if its underlying checks change later.
